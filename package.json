{
  "name": "moda-scraper",
  "version": "1.0.0",
  "description": "Playwright scraper for MODA OS — flights, trains, hotels",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
